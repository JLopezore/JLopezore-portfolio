version: '3.8'

services:
  app:
    build: .
    container_name: portfolio_app
    restart: always
    command: uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
    ports:
      - "8001:8000" # Expuesto solo localmente por seguridad
    volumes:
      - ./app:/app/app